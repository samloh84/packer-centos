# Kickstart file automatically generated by anaconda.

#version=DEVEL
install
url --url=http://mirror.centos.org/centos/$releasever/os/$basearch
repo --name="updates" --baseurl="http://mirror.centos.org/centos/$releasever/updates/$basearch"
repo --name="extras" --baseurl="http://mirror.centos.org/centos/$releasever/extras/$basearch"
repo --name="centosplus" --baseurl="http://mirror.centos.org/centos/$releasever/centosplus/$basearch"
repo --name="epel"  --baseurl="http://download.fedoraproject.org/pub/epel/6/$basearch"
lang en_SG.UTF-8
keyboard us
network --onboot yes --device eth0 --bootproto dhcp --ipv6 auto
rootpw  --iscrypted $6$cRRuIwAFc4utNH7M$/12mqysW/tU5Iy8ZzUA5EVzkc4PVSl.8bI.Krx4VnykxSnGVjgRju1bAMxG8g1bRaqQgsq0fWmTV4e7yLMS8A0
firewall --service=ssh
authconfig --enableshadow --passalgo=sha512
selinux --enforcing
timezone --utc Asia/Singapore
bootloader --location=mbr --driveorder=sda --append="crashkernel=auto rhgb quiet"
# The following is the partition information you requested
# Note that any partitions you deleted are not expressed
# here so unless you clear all partitions first, this is
# not guaranteed to work
#clearpart --all --drives=sda
#volgroup VolGroup --pesize=4096 pv.008002
#logvol / --fstype=ext4 --name=lv_root --vgname=VolGroup --grow --size=1024 --maxsize=51200
#logvol swap --name=lv_swap --vgname=VolGroup --grow --size=1984 --maxsize=1984

#part /boot --fstype=ext4 --size=500
#part pv.008002 --grow --size=1




%packages
@base
@console-internet
@core
@debugging
@directory-client
@hardware-monitoring
@java-platform
@large-systems
@network-file-system-client
@performance
@perl-runtime
@server-platform
@server-policy
@workstation-policy
pax
python-dmidecode
oddjob
sgpio
device-mapper-persistent-data
samba-winbind
certmonger
pam_krb5
krb5-workstation
perl-DBD-SQLite



%post --erroronfail
exec < /dev/tty3 > /dev/tty3
chvt 3

sed '/^%wheel/ s/^#* */# /' -i /etc/sudoers
sed '/%wheel.\+NOPASSWD/ s/^#* *//' -i /etc/sudoers
sed -i 's/\(Defaults\s\+requiretty\)/#\1/g' /etc/sudoers

mkdir -p /root/.ssh
curl -Lj -o /root/.ssh/authorized_keys https://raw.githubusercontent.com/mitchellh/vagrant/master/keys/vagrant.pub
chmod 0600 /root/.ssh/*
chmod 0700 /root/.ssh

mkdir -p /home/vagrant/.ssh
curl -Lj -o /home/vagrant/.ssh/authorized_keys https://raw.githubusercontent.com/mitchellh/vagrant/master/keys/vagrant.pub
chmod 0600 /home/vagrant/.ssh/*
chmod 0700 /home/vagrant/.ssh

chvt 1

%end
